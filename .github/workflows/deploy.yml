name: Deploy to Hostinger

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Deploy via FTP
        uses: SamKirkland/FTP-Deploy-Action@v4.3.5
        with:
          server: 195.35.39.181
          username: u805827262.botshiftnew
          password: ${{ secrets.FTP_PASSWORD }}
          server-dir: /domains/botshift.co/public_html/
          exclude: |
            **/.git*
            **/.git*/**
            **/.DS_Store
            **/legallegendspp/**
            **/resources/**
            **/mediakit/**
            **/*.fig
            **/*.pdf
